# Environment Variables Example
# Copy this file to .env and configure your values

# Backend Configuration (for local development)
NODE_ENV=development
PORT=3001
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend Configuration (for local development)
VITE_API_BASE_URL=http://localhost:3001/api

# Contact Information (NEVER commit these with real values)
VITE_CONTACT_EMAIL=your-email@example.com
VITE_CONTACT_PHONE=+33-X-XX-XX-XX-XX
VITE_CONTACT_LOCATION=Your Location

# Admin Configuration (NEVER use default values in production)
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=password

# Admin Panel Configuration (NEVER use default values in production)
ADMIN_ENABLED=true
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure-admin-password-change-me

# Scheduling System Configuration
ACTION_TOKEN_SECRET=secure-action-token-secret-change-me

# Email Service Configuration (Optional)
VITE_EMAIL_SERVICE_URL=your-email-service-endpoint
EMAIL_API_KEY=your-email-service-api-key
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false

# Production URLs (configured automatically)
# FRONTEND_URL=https://your-site.netlify.app
# VITE_API_BASE_URL=https://your-backend.onrender.com/api

# Note: In production, these will be set automatically:
# - Render.com will set NODE_ENV=production, PORT=10000, and generate JWT_SECRET
# - Netlify will set VITE_API_BASE_URL via netlify.toml
# - CORS will auto-configure based on NODE_ENV